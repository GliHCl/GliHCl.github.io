import { FC } from "react"

export const Separator: FC = () => {
  return (
    <div
      style={{
        width: "100%",
        height: 4,
        borderRadius: 4,
        backgroundColor: "#fff8",
        margin: "16px 0",
      }}
    />
  )
}
